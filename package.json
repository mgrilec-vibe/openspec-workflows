{
  "name": "openspec-workflows-token-opt",
  "version": "1.0.0",
  "description": "Actual-session measurement harness for the BrainSpec slim token-reduction experiment. The harness runs a fixed read-only process-management workload through real OMP model calls, preserves native session JSONL, and compares provider-reported prompt plus completion usage.",
  "private": true,
  "scripts": {
    "test": "node tools/test.mjs",
    "workload:baseline": "node tools/workload.mjs sessions/baseline_session.jsonl baseline",
    "workload:optimized": "node tools/workload.mjs sessions/optimized_session.jsonl slim",
    "measure": "node tools/measure.mjs sessions/baseline_session.jsonl sessions/optimized_session.jsonl"
  }
}
